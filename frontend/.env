# API Configuration for Frontend
# Used by src/environment.js for dynamic backend URL selection

# Production backend URL (used as fallback when not on localhost)
VITE_DEFAULT_API_URL=https://lets-meet-the-video-conferencing-app.onrender.com

# Optional: Override the API URL entirely (if set, this takes precedence)
# VITE_API_URL=https://your-custom-api-url.com
